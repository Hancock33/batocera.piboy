diff --git a/ShadercConfig.cmake.in b/ShadercConfig.cmake.in
index bfdf945..b14294c 100644
--- a/ShadercConfig.cmake.in
+++ b/ShadercConfig.cmake.in
@@ -4,7 +4,7 @@ include("${CMAKE_CURRENT_LIST_DIR}/ShadercTargets.cmake")
 
 check_required_components(Shaderc)
 
-get_target_property(TEMP_SHADERC_LOCATION Shaderc::shaderc_shared LOCATION)
+get_target_property(TEMP_SHADERC_LOCATION Shaderc::shaderc_ds LOCATION)
 message(STATUS "Found Shaderc: ${TEMP_SHADERC_LOCATION}")
 unset(TEMP_SHADERC_LOCATION)
 
diff --git a/libshaderc/CMakeLists.txt b/libshaderc/CMakeLists.txt
index dc881f4..051154b 100644
--- a/libshaderc/CMakeLists.txt
+++ b/libshaderc/CMakeLists.txt
@@ -24,21 +24,21 @@ set(SHADERC_SOURCES
   src/shaderc_private.h
 )
 
-add_library(shaderc_shared SHARED ${SHADERC_SOURCES})
-shaderc_default_compile_options(shaderc_shared)
-target_include_directories(shaderc_shared
+add_library(shaderc_ds SHARED ${SHADERC_SOURCES})
+shaderc_default_compile_options(shaderc_ds)
+target_include_directories(shaderc_ds
     PRIVATE include
             ${glslang_SOURCE_DIR}
             ${SPIRV-Headers_SOURCE_DIR}/include)
-target_compile_definitions(shaderc_shared
+target_compile_definitions(shaderc_ds
     PRIVATE SHADERC_IMPLEMENTATION
-    PUBLIC SHADERC_SHAREDLIB
+    PUBLIC shaderc_dsLIB
 )
 
 # Use a different shared library name on Linux, to avoid conflicts with
 # the shaderc distributed with the Vulkan SDK.
 if(NOT WIN32)
-  set_target_properties(shaderc_shared PROPERTIES OUTPUT_NAME "shaderc_ds")
+  set_target_properties(shaderc_ds PROPERTIES OUTPUT_NAME "shaderc_ds")
 endif()
 
 if(SHADERC_ENABLE_INSTALL)
@@ -52,7 +52,7 @@ if(SHADERC_ENABLE_INSTALL)
     DESTINATION
       ${CMAKE_INSTALL_INCLUDEDIR}/shaderc)
 
-  install(TARGETS shaderc_shared
+  install(TARGETS shaderc_ds
     EXPORT ShadercTargets
     INCLUDES DESTINATION ${CMAKE_INSTALL_INCLUDEDIR}
     LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
@@ -69,11 +69,11 @@ set(SHADERC_LIBS
   SPIRV-Tools
 )
 
-target_link_libraries(shaderc_shared PRIVATE ${SHADERC_LIBS})
+target_link_libraries(shaderc_ds PRIVATE ${SHADERC_LIBS})
 
 shaderc_add_tests(
-  TEST_PREFIX shaderc_shared
-  LINK_LIBS shaderc_shared SPIRV-Tools
+  TEST_PREFIX shaderc_ds
+  LINK_LIBS shaderc_ds SPIRV-Tools
   INCLUDE_DIRS include ${shaderc_SOURCE_DIR}/libshaderc_util/include ${glslang_SOURCE_DIR}
                ${spirv-tools_SOURCE_DIR}/include
                ${SPIRV-Headers_SOURCE_DIR}/include
