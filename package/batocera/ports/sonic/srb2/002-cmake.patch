diff --git a/CMakeLists.txt b/CMakeLists.txt
index 915912af5..28b2e31e4 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -90,12 +90,12 @@ if("${CMAKE_HOST_SYSTEM_NAME}" STREQUAL Windows)
 			message(WARNING "USE_CCACHE was set but ccache is not found (set CCACHE_TOOL_PATH)")
 		endif()
 	endif()
-else()
-	CPMAddPackage(
-		NAME Ccache.cmake
-		GITHUB_REPOSITORY TheLartians/Ccache.cmake
-		VERSION 1.2
-	)
+#else()
+#	CPMAddPackage(
+#		NAME Ccache.cmake
+#		GITHUB_REPOSITORY TheLartians/Ccache.cmake
+#		VERSION 1.2
+#	)
 endif()
 
 # Dependencies
