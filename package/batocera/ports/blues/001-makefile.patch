diff --git a/Makefile b/Makefile
index 1036f7e..e53390d 100644
--- a/Makefile
+++ b/Makefile
@@ -1,6 +1,6 @@
 
-SDL_CFLAGS := `sdl2-config --cflags`
-SDL_LIBS   := `sdl2-config --libs`
+SDL_CFLAGS = `pkg-config sdl2 --cflags`
+SDL_LIBS = `pkg-config sdl2 --libs`
 
 MODPLUG_LIBS ?= -lmodplug
 
