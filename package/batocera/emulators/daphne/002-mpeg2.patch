diff -Naur daphne-v2.5.4.orig/src/CMakeLists.txt daphne-v2.5.4/src/CMakeLists.txt
--- daphne-v2.5.4.orig/src/CMakeLists.txt	2021-06-03 19:49:59.000000000 +0100
+++ daphne-v2.5.4/src/CMakeLists.txt	2021-06-05 14:09:23.768129459 +0100
@@ -46,14 +46,13 @@
 include(GetGitRevisionDescription)
 include(InstallRequiredSystemLibraries)
 include(FindPkgConfig)
-include(ExternalProject)
-include(BuildLibMPEG2)
 
 use_cxx11( )
 
 PKG_SEARCH_MODULE(SDL2 REQUIRED sdl2)
 PKG_SEARCH_MODULE(SDL2_TTF REQUIRED SDL2_ttf)
-build_libmpeg2( )
+PKG_SEARCH_MODULE(SDL2_IMAGE REQUIRED SDL2_image)
+PKG_SEARCH_MODULE(MPEG2 REQUIRED libmpeg2)
 
 message(STATUS "Target: ${CMAKE_SYSTEM_NAME} ${CMAKE_TARGET_ARCHITECTURES}")
 
@@ -81,6 +80,7 @@
 include_directories(
     ${PROJECT_BINARY_DIR} # config.h
     ${SDL2_INCLUDE_DIRS}
+    ${MPEG2_INCLUDE_DIRS}
 )
 
 if( "${CMAKE_TARGET_ARCHITECTURES}" MATCHES "i386" )
@@ -106,18 +106,8 @@
 add_subdirectory(video)
 add_subdirectory(vldp)
 
-add_dependencies( vldp libmpeg2 )
-add_dependencies( ldp-out vldp )
-add_dependencies( game vldp )
-add_dependencies( sound vldp )
-
-find_package(SDL2 REQUIRED)
-find_package(SDL2TTF REQUIRED)
-find_package(SDL2_IMAGE REQUIRED)
-find_package(Vorbis REQUIRED)
-
 add_executable( hypseus hypseus.cpp globals.h )
-target_link_libraries( hypseus plog io timer sound video cpu game ${Vorbis_Enc_LIBRARY} Vorbis::vorbis ${OGG_LIBRARIES} ${VORBISFILE_LIBRARIES} ${Vorbis_File_LIBRARY} ${SDL2_LIBRARIES} ${SDL2TTF_LIBRARY} ${SDL2_IMAGE_LIBRARY} )
+target_link_libraries( hypseus plog io timer sound video cpu game ${SDL2_LIBRARIES} ${SDL2_TTF_LIBRARIES})
 
 if(USB_SCOREBOARD)
     add_library(ftdi1 INTERFACE)
@@ -125,7 +115,7 @@
 
 set_source_files_properties( testvldp.cpp PROPERTIES COMPILE_FLAGS -DSHOW_FRAMES)
 add_executable( testvldp testvldp.cpp )
-target_link_libraries( testvldp video vldp ${SDL2_LIBRARIES} ${SDL2TTF_LIBRARY} ${SDL2IMAGE_LIBRARY})
+target_link_libraries( testvldp video vldp ${SDL2_LIBRARIES} ${SDL2_TTF_LIBRARIES} ${SDL2IMAGE_LIBRARIES})
 
 if(BUILDBOT)
     add_custom_command( TARGET hypseus
diff -Naur daphne-v2.5.4.orig/src/vldp/vldp.h daphne-v2.5.4/src/vldp/vldp.h
--- daphne-v2.5.4.orig/src/vldp/vldp.h	2021-06-03 19:49:59.000000000 +0100
+++ daphne-v2.5.4/src/vldp/vldp.h	2021-06-05 13:56:05.433620262 +0100
@@ -35,6 +35,8 @@
 // But CMake does the right thing here 
 #include <SDL.h> // only used for threading
 
+#include <mpeg2.h>
+
 struct yuv_buf {
     uint8_t *Y;     // Y channel
     uint8_t *U;     // U channel
diff -Naur daphne-v2.5.4.orig/src/vldp/vldp_internal.h daphne-v2.5.4/src/vldp/vldp_internal.h
--- daphne-v2.5.4.orig/src/vldp/vldp_internal.h	2021-06-03 19:49:59.000000000 +0100
+++ daphne-v2.5.4/src/vldp/vldp_internal.h	2021-06-05 13:56:05.433620262 +0100
@@ -27,7 +27,6 @@
 
 #include "vldp.h" // for the VLDP_BOOL definition and SDL.h
 
-#include <mpeg2.h>
 
 // this is which version of the .dat file format we are using
 #define DAT_VERSION 2
