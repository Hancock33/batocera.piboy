diff --git a/src/gui/sdlmain.cpp b/src/gui/sdlmain.cpp
index b138dc9b0..1baceb37d 100644
--- a/src/gui/sdlmain.cpp
+++ b/src/gui/sdlmain.cpp
@@ -818,7 +818,7 @@ const char *modifier;
 #elif defined(HAIKU)
 #define DEFAULT_CONFIG_FILE "~/config/settings/dosbox-x/dosbox-x.conf"
 #else /*linux freebsd*/
-# define DEFAULT_CONFIG_FILE            "/.dosboxrc"
+# define DEFAULT_CONFIG_FILE            "/dosboxx.conf"
 #endif
 
 #if C_SET_PRIORITY
diff --git a/src/misc/cross.cpp b/src/misc/cross.cpp
index 54c92adf8..d2813f65e 100644
--- a/src/misc/cross.cpp
+++ b/src/misc/cross.cpp
@@ -147,7 +147,7 @@ void Cross::GetPlatformConfigDir(std::string& in) {
 #elif !defined(HX_DOS)
 	const char *xdg_conf_home = getenv("XDG_CONFIG_HOME");
 	const std::string conf_home = xdg_conf_home && xdg_conf_home[0] == '/' ? xdg_conf_home: "~/.config";
-	in = conf_home + "/dosbox-x";
+	in = conf_home + "/dosbox";
 	ResolveHomedir(in);
 #endif
 	//LOG_MSG("Config dir: %s", in.c_str());
@@ -160,7 +160,7 @@ void Cross::GetPlatformConfigName(std::string& in) {
 #elif defined(MACOSX)
 #define DEFAULT_CONFIG_FILE "DOSBox-X " VERSION " Preferences"
 #else /*linux freebsd*/
-#define DEFAULT_CONFIG_FILE "dosbox-x-" VERSION ".conf"
+#define DEFAULT_CONFIG_FILE "dosboxx.conf"
 #endif
 	in = DEFAULT_CONFIG_FILE;
 }
@@ -184,7 +184,7 @@ void Cross::CreatePlatformConfigDir(std::string& in) {
 #elif !defined(HX_DOS)
 	const char *xdg_conf_home = getenv("XDG_CONFIG_HOME");
 	const std::string conf_home = xdg_conf_home && xdg_conf_home[0] == '/' ? xdg_conf_home: "~/.config";
-	in = conf_home + "/dosbox-x";
+	in = conf_home + "/dosbox";
 	ResolveHomedir(in);
 	mkdir(in.c_str(),0700);
 #endif
