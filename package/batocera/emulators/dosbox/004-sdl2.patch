diff --git a/configure.ac b/configure.ac
index 407d553..a047bef 100644
--- a/configure.ac
+++ b/configure.ac
@@ -32,11 +32,11 @@ LIBS="$LIBS $SDL_LIBS"
 CPPFLAGS="$CPPFLAGS $SDL_CFLAGS"
 
 dnl Check if SDL is 2.0.x
-AC_MSG_CHECKING([for SDL version being 2.0.x])
+AC_MSG_CHECKING([for SDL version being 2.24.x])
 AC_COMPILE_IFELSE([AC_LANG_SOURCE([
 #include "SDL.h"
 void blah(){
-#if !((SDL_MAJOR_VERSION == 2) && (SDL_MINOR_VERSION == 0))
+#if !((SDL_MAJOR_VERSION == 2) && (SDL_MINOR_VERSION == 24))
 #error "Only SDL 2.0 supported"
 #endif
 ;
