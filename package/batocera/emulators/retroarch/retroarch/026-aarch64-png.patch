diff --git a/libretro-common/formats/png/rpng.c b/libretro-common/formats/png/rpng.c
index 91c17c0d72..30631fe5ca 100644
--- a/libretro-common/formats/png/rpng.c
+++ b/libretro-common/formats/png/rpng.c
@@ -36,10 +36,10 @@
 #include <emmintrin.h>
 #define RPNG_SIMD_SSE2 1
 #elif defined(__ARM_NEON) || defined(__ARM_NEON__)
-#if !defined(VITA) && !defined(WEBOS) && !defined(HAVE_LIBNX)
+/*#if !defined(VITA) && !defined(WEBOS) && !defined(HAVE_LIBNX)
 #include <arm_neon.h>
 #define RPNG_SIMD_NEON 1
-#endif
+#endif*/
 #endif
 
 #include <boolean.h>
