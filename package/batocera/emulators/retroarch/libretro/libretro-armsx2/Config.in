config BR2_PACKAGE_LIBRETRO_ARMSX2
	bool "libretro-armsx2"
	depends on BR2_INSTALL_LIBSTDCPP

	help
	  A libretro core for PS2

	  http://www.libretro.com

comment "LIBRETRO_ARMSX2 needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
