diff --git a/Makefile b/Makefile
index 2214802..83901e1 100644
--- a/Makefile
+++ b/Makefile
@@ -87,6 +87,36 @@ else ifneq (,$(findstring rockchip,$(platform)))
        GLES31 := 1
    endif
 
+# Raspberry Pi 5 (64-bit)
+else ifeq ($(platform), rpi5_64)
+    TARGET := $(TARGET_NAME)_libretro.so
+	fpic := -fPIC
+    SHARED := -shared -Wl,--version-script=$(CORE_DIR)/link.T -Wl,--no-undefined
+	CFLAGS += -mcpu=cortex-a76 -mtune=cortex-a76 -ffast-math -DARM
+    CFLAGS += -D_POSIX_C_SOURCE=199309L
+    GLES := 1
+    GLES31 := 1
+
+# Raspberry Pi 4 (64-bit)
+else ifeq ($(platform), rpi4_64)
+    TARGET := $(TARGET_NAME)_libretro.so
+	fpic := -fPIC
+    SHARED := -shared -Wl,--version-script=$(CORE_DIR)/link.T -Wl,--no-undefined
+	CFLAGS += -mcpu=cortex-a72 -mtune=cortex-a72 -Wno-implicit-function-declaration -Wno-int-conversion -ffast-math -DARM
+    CFLAGS += -D_POSIX_C_SOURCE=199309L
+    GLES := 1
+    GLES31 := 1
+
+# Raspberry Pi 3 (64-bit)
+else ifeq ($(platform), rpi3_64)
+    TARGET := $(TARGET_NAME)_libretro.so
+	fpic := -fPIC
+    SHARED := -shared -Wl,--version-script=$(CORE_DIR)/link.T -Wl,--no-undefined
+	CFLAGS += -mcpu=cortex-a53 -mtune=cortex-a53 -ffast-math -DARM
+    CFLAGS += -D_POSIX_C_SOURCE=199309L
+    GLES := 1
+    GLES31 := 1
+
 else ifneq (,$(findstring osx,$(platform)))
    TARGET := $(TARGET_NAME)_libretro.dylib
    fpic := -fPIC
@@ -247,7 +277,7 @@ endif
 ifeq ($(GLES), 1)
    HAVE_OPENGL = 0
    CFLAGS += -DHAVE_OPENGLES
-   GL_LIB += -lGLESv2
+   GL_LIB = -lGLESv2
    ifeq ($(GLES31), 1)
       CFLAGS += -DHAVE_OPENGLES3 -DHAVE_OPENGLES_3_1
    else ifeq ($(GLES3), 1)
