diff --git a/cmake/StandardProjectSettings.cmake b/cmake/StandardProjectSettings.cmake
index 4e1a9b31..24babcc1 100644
--- a/cmake/StandardProjectSettings.cmake
+++ b/cmake/StandardProjectSettings.cmake
@@ -131,7 +131,6 @@ if(AMIBERRY_GNU_LIKE_COMPILER)
             # GNU ld-only flags (do not work on FreeBSD or Windows)
             if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
                 list(APPEND AMIBERRY_LINK_OPTIONS
-                    "-Wl,--sort-common=descending"
                     "-Wl,--hash-style=gnu"
                 )
             endif()
