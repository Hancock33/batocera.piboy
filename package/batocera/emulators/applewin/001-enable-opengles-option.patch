diff --git a/source/frontends/sdl/CMakeLists.txt b/source/frontends/sdl/CMakeLists.txt
index 03d2bad2..81ddc258 100644
--- a/source/frontends/sdl/CMakeLists.txt
+++ b/source/frontends/sdl/CMakeLists.txt
@@ -49,12 +49,7 @@ else()
   )
 endif()
 
-if ((${CMAKE_SYSTEM_NAME} MATCHES "Darwin") OR (${CMAKE_SYSTEM_NAME} MATCHES "Windows"))
-  # only OpenGL supported on MacOS and Windows
-  set (SA2_OPENGL ON)
-else()
-  option(SA2_OPENGL "Prefer OpenGL over OpenGL ES" OFF)
-endif()
+option(SA2_OPENGL "Prefer OpenGL over OpenGL ES" OFF)
 
 if (SA2_OPENGL)
   find_package(OpenGL REQUIRED)
@@ -91,6 +86,7 @@ target_include_directories(sa2 PRIVATE
   ${GLES2_INCLUDE_DIRS}
   )
 
+if (SA2_OPENGL)
 target_link_libraries(sa2 PRIVATE
   appleii
   common2
@@ -98,9 +94,24 @@ target_link_libraries(sa2 PRIVATE
   ${PCAP_LIBRARIES}
   ${SLIRP_LIBRARIES}
 
-  ${GLES2_LIBRARIES}
   OpenGL::GL
   )
+else()
+target_link_libraries(sa2 PRIVATE
+  appleii
+  common2
+
+  ${YAML_LIBRARIES}
+  ${MINIZIP_LIBRARIES}
+  ${PCAP_LIBRARIES}
+  ${SLIRP_LIBRARIES}
+  ${ZLIB_LIBRARIES}
+
+  ${SDL2_LIBRARIES}
+  ${SDL2_IMAGE_LIBRARIES}
+  ${GLES2_LIBRARIES}
+  )
+endif()
 
 target_link_directories(sa2 PRIVATE
   ${GLES2_LIBRARY_DIRS}
