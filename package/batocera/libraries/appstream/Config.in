config BR2_PACKAGE_APPSTREAM
	bool "appstream"
	select BR2_PACKAGE_BASH_COMPLETION
	select BR2_PACKAGE_LIBFYAML
	select BR2_PACKAGE_LIBXMLB

	help
	  AppStream makes machine-readable software metadata
	  easily accessible.
	  This library provides objects and helper methods to
	  help reading and writing AppStream metadata.

	  https://github.com/ximion/appstream
