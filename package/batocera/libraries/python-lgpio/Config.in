config BR2_PACKAGE_PYTHON_LGPIO
	bool "python-lgpio"
	select BR2_PACKAGE_LIBLGPIO
	help
	  lgpio is a module for Linux Single Board Computers
	  which allows control of the General Purpose Input Outputs.

	  https://pypi.org/project/lgpio
