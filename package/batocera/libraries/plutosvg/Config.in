config BR2_PACKAGE_PLUTOSVG
	bool "plutosvg"
	depends on BR2_INSTALL_LIBSTDCPP

	help
	  plutosvg is a tiny SVG rendering library in C

	  https://github.com/stenzek/plutosvg

comment "BR2_PACKAGE_PLUTOSVG needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
