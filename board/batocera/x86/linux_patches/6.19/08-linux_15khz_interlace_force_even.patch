diff --git a/drivers/gpu/drm/amd/display/dc/dio/dcn10/dcn10_stream_encoder.c b/drivers/gpu/drm/amd/display/dc/dio/dcn10/dcn10_stream_encoder.c
index d928b4dcf6b8..1be7ef4b935e 100644
--- a/drivers/gpu/drm/amd/display/dc/dio/dcn10/dcn10_stream_encoder.c
+++ b/drivers/gpu/drm/amd/display/dc/dio/dcn10/dcn10_stream_encoder.c
@@ -263,6 +263,10 @@ void enc1_stream_encoder_dp_set_stream_attribute(
 	struct dc_crtc_timing hw_crtc_timing = *crtc_timing;
 
 	if (hw_crtc_timing.flags.INTERLACE) {
+		/*DCN needs even vertical fields*/
+		hw_crtc_timing.v_total = (hw_crtc_timing.v_total + 1) & ~1;
+		hw_crtc_timing.v_front_porch = (hw_crtc_timing.v_front_porch + 1) & ~1;
+		hw_crtc_timing.v_sync_width = (hw_crtc_timing.v_sync_width + 1) & ~1;
 		/*the input timing is in VESA spec format with Interlace flag =1*/
 		hw_crtc_timing.v_total /= 2;
 		hw_crtc_timing.v_border_top /= 2;
diff --git a/drivers/gpu/drm/amd/display/dc/hwss/dcn10/dcn10_hwseq.c b/drivers/gpu/drm/amd/display/dc/hwss/dcn10/dcn10_hwseq.c
index 4c4d0c802f9b..e472c96c6553 100644
--- a/drivers/gpu/drm/amd/display/dc/hwss/dcn10/dcn10_hwseq.c
+++ b/drivers/gpu/drm/amd/display/dc/hwss/dcn10/dcn10_hwseq.c
@@ -3915,6 +3915,10 @@ static void apply_front_porch_workaround(
 	if (timing->flags.INTERLACE == 1) {
 		if (timing->v_front_porch < 2)
 			timing->v_front_porch = 2;
+
+		timing->v_front_porch = (timing->v_front_porch + 1) & ~1;
+		timing->v_total = (timing->v_total + 1) & ~1;
+
 	} else {
 		if (timing->v_front_porch < 1)
 			timing->v_front_porch = 1;
diff --git a/drivers/gpu/drm/amd/display/dc/optc/dcn10/dcn10_optc.c b/drivers/gpu/drm/amd/display/dc/optc/dcn10/dcn10_optc.c
index bc98fde47ee8..585dbc7a91ae 100644
--- a/drivers/gpu/drm/amd/display/dc/optc/dcn10/dcn10_optc.c
+++ b/drivers/gpu/drm/amd/display/dc/optc/dcn10/dcn10_optc.c
@@ -54,6 +54,10 @@ static void apply_front_porch_workaround(struct dc_crtc_timing *timing)
 	if (timing->flags.INTERLACE == 1) {
 		if (timing->v_front_porch < 2)
 			timing->v_front_porch = 2;
+
+		timing->v_front_porch = (timing->v_front_porch + 1) & ~1;
+		timing->v_total = (timing->v_total + 1) & ~1;
+
 	} else {
 		if (timing->v_front_porch < 1)
 			timing->v_front_porch = 1;
