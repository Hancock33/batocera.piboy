diff --git a/src/joystick/linux/SDL_sysjoystick.c b/src/joystick/linux/SDL_sysjoystick.c
index e6db86781..dc565494d 100644
--- a/src/joystick/linux/SDL_sysjoystick.c
+++ b/src/joystick/linux/SDL_sysjoystick.c
@@ -1039,7 +1039,7 @@ static void ConfigJoystick(SDL_Joystick *joystick, int fd)
                 ++joystick->nhats;
             }
         }
-        for (i = 0; i < ABS_MAX; ++i) {
+        for (i = 0; i < ABS_MISC; ++i) {
             /* Skip digital hats */
             if (joystick->hwdata->has_hat[(i - ABS_HAT0X) / 2]) {
                 continue;
@@ -1517,6 +1517,9 @@ static void HandleInputEvents(SDL_Joystick *joystick)
                                           events[i].value);
                 break;
             case EV_ABS:
+                if (code >= ABS_MISC) {
+                    break;
+                }
                 switch (code) {
                 case ABS_HAT0X:
                 case ABS_HAT0Y:
